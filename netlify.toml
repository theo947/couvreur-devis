[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Route propre vers la fonction de dépôt de lead
[[redirects]]
  from = "/api/lead"
  to = "/.netlify/functions/lead"
  status = 200
